language: node_js
node_js:
 — “10.13”
script:
 — echo “Deploy to Firebase”
install:
 — npm install -g firebase-tools
after_success:
 — firebase deploy — project eight-ball-answers — token $FIREBASE_TOKEN.